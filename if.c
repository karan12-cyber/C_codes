#include<stdio.h>
int main()
 { 
   int num;
    
    printf("girls are best"); 
      if num>5;
      printf("boys are best");
       if num<5;
        printf("both are best");
         if num =5;
          return 0; 
 }
